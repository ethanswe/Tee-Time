const db = require('../db/models');
const { asyncHandler } = require('./utils')



document.querySelector('.course-profile-button').addEventListener('click', async (e) => {
    const loadInfo = async () => {
        
    }
}) 


// button event listener on click
// asynchronous function to retreieve the specific course info from database
// re-route to create event page with the course name already populated 